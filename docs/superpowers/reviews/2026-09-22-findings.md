# GIDrone review findings — started 2026-09-22

Severity: S1 = data wrong or security hole, S2 = feature broken, S3 = bug with workaround, S4 = cleanup.
Status: open | fixing | fixed | wontfix

| # | Sev | Area | File:line | Finding | Fix | Status |
|---|-----|------|-----------|---------|-----|--------|
| 1 | S1 | security | src/services/weatherService.js:7 | OpenWeatherMap key shipped in browser bundle | move calls behind an Edge Function or accept and cap usage | open |
| 2 | S1 | security | supabase/functions/_shared/aviation.ts:3 | isOriginAllowed always true, CORS `*` on every function | allow-list gi-drone.vercel.app and localhost | open |
| 3 | S1 | security | supabase/functions/* | verify_jwt false on all 9 functions, no rate limiting | require anon JWT, add per-IP limit in shared helper | open |
| 4 | S1 | security | supabase secrets | NOTAM_SYNC_TOKEN, OBSTRUCTION_SYNC_TOKEN, NOTAM_INGEST_TOKEN unset, so anyone can trigger syncs | set all three, send from cron and CI | open |
| 5 | S1 | security | Supabase project cakmsciuqaodlgzbrcfu | shared with trading/budgeting apps incl. bank_accounts and plaid_items tables | decide: separate project or strict RLS review across all tables | open |
| 6 | S2 | cost | src/App.tsx:456 | tab switch destroys and recreates Mapbox map (billed load each time) | see map cost plan | open |
| 7 | S3 | deps | package-lock.json | 14 high npm audit findings | `npm audit fix` applied 2026-09-22, 0 remaining incl. dev, build verified | fixed |
| 8 | S1 | security | public.rls_auto_enable() | SECURITY DEFINER, executable by anon and authenticated via /rest/v1/rpc | revoked from anon, authenticated, public (migration 20260922000000). Verified has_function_privilege false for both | fixed |
| 9 | S3 | security | public.prune_notam_feed() | role-mutable search_path (advisor 0011); also executable by anon, which lets anyone trigger pruning | `alter function public.prune_notam_feed() set search_path = public;` and revoke from anon | open |
| 10 | S3 | perf | sites, site_notes RLS policies | `auth.uid()` re-evaluated per row (advisor 0003) on all 7 own-row policies | rewrite each policy to use `(select auth.uid())` | open |
| 11 | S4 | perf | site_notes.user_id | foreign key without covering index | `create index on site_notes(user_id);` | open |
| 12 | S3 | security | Supabase Auth | leaked-password protection disabled | enable in Auth settings (only matters if email+password is ever enabled; app uses OAuth and magic link) | open |
| 13 | S3 | code | RadarTab.tsx:1383, :1493; ConditionsTab.tsx:1124, :2732, :2749; App.tsx:424; useGlobalLocation.ts:200, :209, :238; SitesTab.tsx:95, :109; SiteDetail.tsx:103; MySitesTab.tsx:206; GlobalLocationBar.tsx:57 | 14x `react-hooks/set-state-in-effect`: synchronous setState inside useEffect causes a second render per effect run | derive the value during render or move to event handler; check each for a real double-fetch | open |
| 14 | S4 | code | RadarTab.tsx:75, :1088, :1092 | empty catch blocks swallow errors silently | log at debug level or comment why ignoring is safe | open |
| 15 | S4 | code | RadarTab.tsx:356 | unnecessary escape in string | remove `\` | open |
| 16 | S3 | code | 27 sites (eslint `exhaustive-deps` warnings) | effects reading values not in their dependency arrays; stale-closure risk | review each in Phase B; RadarTab and ConditionsTab first | open |

## Lint baseline (2026-09-22, eslint 9 + typescript-eslint 8 + react-hooks 7)

54 problems: 18 errors, 36 warnings.
Errors by rule: set-state-in-effect 14, no-empty 3, no-useless-escape 1.
Warnings by rule: exhaustive-deps 27, no-explicit-any 4, no-unused-vars 4, only-export-components 1.

## Dependency audit (2026-09-22)

Before: 20 advisories total (13 high, 5 moderate, 2 low). Production-only: 2 (ws high, protocol-buffers-schema moderate).
After `npm audit fix` (no `--force`): 0 total. `npm run build` exits 0. 81 packages changed, all within semver range.

## RLS truth table (2026-09-22), GIDrone tables only

| table | rls | policies | verdict |
|---|---|---|---|
| sites | on | select/insert/update/delete own (auth.uid) | correct |
| site_notes | on | select/insert/delete own; no update policy | correct: app never updates notes (SitesTab.tsx uses insert, delete, select only) |
| notam_feed | on | select for all | correct: public aviation data, writes only via service role |
| obstructions | on | select for all | correct |
| api_cache | on | select for all | acceptable: cached upstream payloads only; confirm no secrets are ever cached (notam-sync caches the NMS OAuth token under key `nms-api:oauth-token`, which IS readable by anon. See #17) |
| stations_index | on | select for all | correct |

| 17 | S1 | security | supabase/functions/notam-sync/index.ts:89, api_cache table | NMS-API OAuth bearer token is cached in `api_cache`, which has a select-all RLS policy, so any anon client can read `select payload from api_cache where cache_key='nms-api:oauth-token'` and use the token against NMS-API for up to 25 min | policy `api_cache_select_all` now filters `cache_key not like 'nms-api:%'` (migration 20260922000000). Verified: anon sees 0 token rows, 7378 other rows; notam-sync still 200 | fixed |

Tables belonging to the other apps in this project (orders, positions, audit_log, plaid_*, bank_accounts, trade_*, etc.) all have RLS on with either zero policies (deny-all to the API) or service-role-only policies. Not reachable from the anon key. Blast radius of #5 is therefore limited to a leaked service-role key, not the anon key.

## Phase A status

Complete 2026-09-22. Tasks 1-4 done; #7, #8, #17 fixed. Next: Phase B (Claude review by path) in a fresh session.
